interface:
  display_name: "RAS App Testing"
  short_description: "Launch and retain isolated RAS Code test environments"
  default_prompt: "Use $test-ras-app to launch an isolated RAS Code environment and iteratively test it in the browser while preserving state."
